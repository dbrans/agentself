# Open Issues

- Decide whether ad-hoc MCP installs should be allowed or restricted to `mcp.json`.
- Decide log file behavior on startup (truncate vs timestamped vs rotate).
- Consider cleaning up attach socket on shutdown.
- Choose default safe root for `harness-start.sh` in sandboxed environments.
